# WSB_BalloonOut 프로젝트 작업 히스토리

## 프로젝트 개요

**프로젝트명:** Arrow Puzzle: Arrow Maze (가제)
**시작일:** 2026-01-22
**현재 버전:** v6.3 (Prototype Algorithm)

### 핵심 컨셉
화면을 95% 가득 채운 화살표들이 서로 얽히고설킨 '화살표 미로'를 풀어내는 Solution-first 퍼즐 게임

---

## 커밋 히스토리

| 날짜 | 커밋 | 작성자 | 내용 |
|------|------|--------|------|
| 2026-01-22 | 25b3af3 | DoubleBK | Unity용 .gitignore 생성 |
| 2026-01-22 | 2193b45 | BK | 알고리즘 프로토타입 구현 테스트 |

---

## 작업 내용 상세

### Phase 1: 프로젝트 설정 (2026-01-22)

#### 1.1 Git 저장소 초기화
- Unity 프로젝트용 `.gitignore` 설정
- 빌드 파일, 캐시, IDE 설정 파일 등 제외 규칙 정의

#### 1.2 PRD 문서 작성 (`PRD.md`)
- **버전:** 0.2.0 (Major Update)
- **핵심 메카닉 정의:**
  - Arrow Modeling: Body Occupancy (길이 = 몸통 점유 길이)
  - Escape Logic: Head에서 화면 끝까지 경로에 장애물이 없으면 탈출
  - Queue & Targeting Logic: Lane 기반 색상 매칭 시스템
- **레벨 디자인 파이프라인:**
  - Solution-first 방식 (해답을 먼저 설계하고 Grid 채움)
  - 목표 Fill Rate 95%
  - 역설계 배치 + Auto Fill + Verifier

---

### Phase 2: 알고리즘 개발 (2026-01-22)

#### 2.1 v1: DAG 기반 의존성 설계 (실패)
- **접근:** Directed Acyclic Graph + 위상 정렬
- **결과:** 생성 실패율 100%
- **문제:** 의존성 조건을 만족하는 물리적 배치 불가

#### 2.2 v2: 분기 의존성 + 완화된 조건 (실패)
- **접근:** 선형 → 분기 의존성, 조건 50% 완화
- **결과:** 생성 실패율 ~95%
- **문제:** 화살표 수 × 평균 길이 > 그리드 용량

#### 2.3 v3: 랜덤 배치 + 검증 (부분 성공)
- **접근:** 완전 랜덤 배치 후 시뮬레이션 검증
- **결과:** 생성 실패율 ~80%
- **문제:** 랜덤 배치로는 풀 수 있는 퍼즐 생성 어려움

#### 2.4 v4: 체인 배치 (실패)
- **접근:** 모든 화살표 같은 방향, 수직/수평 정렬
- **결과:** 생성 실패율 100%
- **문제:** Head/Body 좌표 계산 오류 (범위 벗어남)

#### 2.5 v5: 단순 그리드 배치 (성공)
- **접근:** 모든 화살표 오른쪽(→), 각 행에 하나씩
- **결과:** 생성 성공률 100%
- **특징:** 퍼즐 난이도 낮음, 기본 동작 검증 가능

#### 2.6 v6: 순차적 의존성 배치 (개선)
- **접근:** "먼저 배치한 화살표가 나중 화살표의 경로를 막음"
- **결과:** 생성 성공률 ~90%
- **특징:**
  - 첫 번째 화살표: 즉시 탈출 가능
  - 이후 화살표: 이전 화살표에 의해 막힘
  - 다양한 방향(U/D/L/R) 지원
  - 폴백 로직으로 실패 최소화

---

### Phase 3: 버그 수정 (2026-01-22)

#### 3.1 v6 함수명 충돌 버그
- **에러:** `Cannot read properties of undefined (reading 'valid')`
- **원인:** `validateLevel` 함수가 `generator.js`와 `game.js` 양쪽에 존재, 덮어쓰기 발생
- **해결:** `generator.js`에서 `validateGeneratedLevel`로 함수명 변경

---

### Phase 4: 게임 로직 개선 (2026-01-22)

#### 4.1 v6.1: 충돌 시스템
- **변경사항:**
  - 모든 화살표 클릭 가능 (비활성화 상태 제거)
  - 충돌 시 바운스 백 애니메이션
- **구현:**
  - `getDistanceToCollision()`: 충돌 거리 계산
  - `tryEscape()`: 충돌 감지 및 애니메이션 처리

---

### Phase 5: UI 개선 (2026-01-22)

#### 5.1 v6.2: UI 요소 추가
1. **화살표 순서 번호 표시**
   - 각 화살표에 탈출 순서 번호 오버레이
   - `.arrow-order` CSS 클래스로 스타일링

2. **Solution Order 박스**
   - 화면 하단에 정답 순서 시각화
   - 탈출한 화살표는 `popped` 클래스로 표시

3. **Restart 버튼**
   - 생성된 레벨 재시작 기능
   - 레벨 데이터 저장 및 복원

4. **Generator UI**
   - Grid Size, Queue Lanes, Balloons per Lane 설정
   - Miss Arrows 수 조절
   - Branching Mode (분기 모드) 옵션

#### 5.2 v6.3: Solution Order 버그 수정
- **문제:** Solution Order가 배치 순서를 표시 (실제 클릭 순서와 다름)
- **원인:** `order = idx + 1` (배치 순서)
- **해결:** `validateGeneratedLevel`에서 실제 탈출 순서 반환하여 사용

---

## 파일 구조

```
WSB_BalloonOut/
├── .gitignore              # Unity 프로젝트 설정
└── PrototypeAlgorithm/     # 웹 기반 프로토타입
    ├── index.html          # 메인 UI (Generator 모달 포함)
    ├── style.css           # 스타일시트 (351줄)
    ├── game.js             # 게임 로직 (399줄)
    │   ├── 렌더링 (Grid, Queue, Solution)
    │   ├── 상호작용 (클릭, 탈출, 충돌)
    │   └── Generator UI 통합
    ├── generator.js        # 레벨 생성기 v6 (469줄)
    │   ├── Queue/Color 시퀀스 생성
    │   ├── 순차적 의존성 배치
    │   └── 레벨 검증 (시뮬레이션)
    ├── PRD.md              # 프로토타입 요구사항 문서
    ├── algorithm-history.md # 알고리즘 개발 히스토리
    └── bug-report-v6.md    # v6 버그 리포트
```

---

## 기술 스택

- **Frontend:** Vanilla HTML/CSS/JavaScript
- **빌드 도구:** 없음 (정적 파일)
- **게임 엔진:** (향후) Unity 예정

---

## 핵심 교훈

1. **단순함이 최선:** 복잡한 DAG보다 단순한 그리드 배치가 안정적
2. **제약 조건 균형:** 너무 많으면 배치 불가, 너무 적으면 퍼즐이 쉬움
3. **물리적 한계 고려:** 총 필요 셀 < 그리드 용량 × 0.5
4. **좌표 계산 주의:** Head/Body 방향 관계와 경계 조건 검증 필수
5. **함수명 충돌 주의:** 여러 파일에서 같은 이름 사용 시 덮어쓰기 발생

---

## 버전 요약표

| 버전 | 알고리즘 | 성공률 | 상태 |
|------|----------|--------|------|
| v1 | DAG + 역순 배치 | 0% | 폐기 |
| v2 | 분기 DAG + 완화 | 5% | 폐기 |
| v3 | 랜덤 + 검증 | 20% | 폐기 |
| v4 | 체인 배치 | 0% | 폐기 |
| v5 | 단순 그리드 | 100% | 참조용 |
| **v6** | **순차적 의존성** | **~90%** | **현재 사용** |
| v6.1 | 충돌 바운스 | - | 게임 로직 |
| v6.2 | UI 개선 | - | 완료 |
| v6.3 | Solution 버그 수정 | - | 완료 |

---

## 향후 계획

1. **밀도 향상:** 현재 ~20% → 목표 70%+
2. **의존성 강화:** 더 복잡한 퍼즐 생성
3. **Unity 포팅:** 웹 프로토타입 → Unity 게임
4. **에디터 개발:** PRD에 명시된 Level Editor 구현

---

*문서 작성일: 2026-02-02*
*정리 기준: 커밋 히스토리 및 코드 분석*
